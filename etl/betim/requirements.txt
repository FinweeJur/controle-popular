basedosdados>=2.0
google-cloud-bigquery>=3.0
requests>=2.31
playwright>=1.44
psycopg[binary]>=3.1
pydantic>=2.7
tenacity>=8.3
python-dotenv>=1.0
openpyxl>=3.1
# HTML server-rendered (Drupal 7 da CMBH, etl/camaras/bh.py). Playwright
# resolveria, mas subir um Chromium para ler markup estático custaria minutos
# por rodada; lxml lê os 3,6 mil fragmentos de busca em segundos.
lxml>=5.0
# WAF da GoCache nos dois portais da PBH (ckan.pbh.gov.br e grp.pbh.gov.br):
# bloqueia por fingerprint de TLS, então `requests` leva 403 em tudo, com
# qualquer User-Agent. curl_cffi usa curl-impersonate e reproduz o
# ClientHello de um Chrome real. Ver etl/pbh/cliente.py.
curl_cffi>=0.7
