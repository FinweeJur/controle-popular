# Plano consolidado — 15/08/2026

> Reúne num lugar só tudo que a sessão de hoje levantou: pedidos do dono,
> pendências achadas por medição, e o que os oito planos do repositório ainda
> não entregaram. **Ordenado por custo × benefício**, não por assunto.
>
> Números medidos hoje, contra os arquivos servidos. Onde não medi, está dito.

---

## O gargalo que domina tudo

**O site está congelado desde 13/08 por volta das 22h.** Nove commits de hoje
estão no GitHub e nenhum está no ar.

Não é falta de trabalho: é que `next build` lê a **Neon**, que está em
**HTTP 402 até 01/09**. Sem banco, não há build; sem build, não há deploy.

O caminho documentado para furar isso é `docs/build-em-outro-pc.md`: banco e
build na máquina do dono, Cloudflare só serve. **Nunca foi executado de ponta
a ponta.**

### Estado da tentativa de hoje

| Máquina | Papel | Situação medida (15/08) |
|---|---|---|
| `desktop-fefpddp` (onde a sessão roda) | dado bruto, venvs | **não tem** cópia do banco de produção |
| `home-pc` (100.91.10.1) | Postgres local, build e deploy | **online**, `tailscale ping` responde em 4 ms |

O servidor está alcançável pela tailnet, mas **nenhuma porta de serviço
responde** — 22, 445, 3389 e 5432 todas fechadas. Dá para falar com a máquina,
não dá para mandar nela.

**O único dump nesta máquina é de 28/07** (`X:\DevCoder\_migracao-neon\dump\cp.dump`,
3,8 MiB). Buildar com ele publicaria o site com **três semanas de dado a menos**
— pior que não publicar. Não é caminho.

### Como publicar hoje, então

Uma destas três, em ordem de esforço:

1. **Rodar o build no `home-pc`** (ele tem o Postgres atual). É onde a
   arquitetura já foi desenhada para funcionar. Uma linha:
   `git pull && npm run rotina-local` — conferir o nome exato do script em
   `docs/rotina-local.md` antes.
2. **Abrir o OpenSSH no `home-pc`** e me deixar rodar de lá. Custo: uma
   configuração, uma vez. Ganho: publicar deixa de depender de quem está na
   frente de qual teclado.
3. **Trazer um dump atual do `home-pc` para cá** e buildar aqui. Só vale se o
   servidor não puder buildar, porque duplica o banco em duas máquinas — o que
   `docs/MAQUINAS.md` existe justamente para evitar.


---

## ✅ Executado nesta sessão (26 commits, `e82a58e..246f6c9`)

Faixa 1 e boa parte da Faixa 2 saíram. O que **não** saiu é o item 1 — publicar —,
e ele continua sendo o gargalo de tudo.

| Item | Estado |
|---|---|
| Ressalva de coleta em curso (3 páginas) | ✅ `964d7ca` |
| Categoria própria do Brumadinho no painel | ✅ `42aeda4` |
| Imagem de satélite por zoom no globo publicado | ✅ `e8b2c5c` |
| Dica de 2 s ao pousar o mouse | ✅ `33272c6` + verificada em `15be710` |
| Movimento (MAB) na hero de quem-atua | ✅ `430d038` |
| Clipping das ATIs (46) + filtros | ✅ `3313175` |
| Clipping das IJs (59) | ✅ `5e9fb75` |
| Biblioteca das assessorias (597) | ✅ `246f6c9` |
| Radar diário de notícias | ✅ `bfa8669` (falta ligar na tela) |
| Unificação de Curvelo (12 áreas duplicadas) | ✅ `94be872` |
| Imóveis da União: são 25, não 24 | ✅ `14e99c4` |
| Risco climático (AdaptaBrasil + INMET) | 🟡 `381b467` — coletado, **não carregado** |
| Legislação federal (8.940 normas) | 🟡 `0d8480b` — código pronto, **não carregado** |
| **Publicar** | ⛔ **não** |

## O que a sessão acrescentou à fila

- **Índice climático não pode ir sozinho para a tela** (caso BH — ver o log da Wiki).
- **Página de educação do painel-fonte**: 48 itens nunca ingeridos.
- **Ligar o radar e o clipping das IJs** nas telas.
- ⚠️ **O painel `(V1)` em disco é mais velho que o portal** (130 notícias contra 149,
  12 marcos contra 17). Regerar os arquivos existentes a partir dele **apagaria dado**.

---

## Ranking por custo × benefício

Critério: **benefício** é quanto muda para quem lê o site; **custo** é tempo e
risco. Empate desempata pelo que destrava outras tarefas.

### 🥇 Faixa 1 — muito benefício, custo baixo

| # | Tarefa | Por que primeiro | Custo |
|---|---|---|---|
| 1 | **Publicar** (qualquer um dos três caminhos acima) | Nove commits, quatro camadas de alerta e a legislação federal só existem no git. Sem isto, todo o resto de hoje é trabalho não entregue | 20 min no servidor |
| 2 | **Ressalva de dado parcial nas páginas** | Pedido do dono, e o mais barato de todos: é texto. Hoje páginas afirmam número sem dizer que a coleta está em curso. Foi escrito por engano no repositório errado e precisa ser refeito aqui | 1 h |
| 3 | **Protocolo do INCRA no Fala.BR** | **Vence em 18/08 — 3 dias.** O registro já existe (`docs/LAI-PROTOCOLOS.json`) e acusa que o número nunca foi anotado. É só localizar e preencher | 15 min |
| 4 | **Categoria própria para Brumadinho no painel** | A seção de território tem 21 linhas, 8 delas são do rompimento. Separar é reordenar entradas | 30 min |

### 🥈 Faixa 2 — muito benefício, custo médio

| # | Tarefa | Por que | Custo |
|---|---|---|---|
| 5 | **Carregar as 8.940 normas federais** no banco que serve o site | O código está pronto e testado; sem a carga, `/ambiental/legislacao` mostra **0 nacionais**. É o que faz a proteção animal aparecer | 1 h, depende de 1 |
| 6 | **Tooltip de 2 s ao passar o mouse numa área** | Pedido do dono. Hoje, saber o que é uma mancha exige clicar. Custo real está em decidir o texto: uma frase, não a ficha | 3 h |
| 7 | **Portar a imagem de satélite por zoom** | Única entrega de ontem que não existe no globo publicado. O modo natural (Esri) não precisa de servidor; o do INPE precisaria de rota `.din.ts` | 2 h |
| 8 | **13 territórios quilombolas e as 103 barragens sem mancha** | Lacunas com número. A primeira já mudou um alerta de zero para seis — a cobertura ainda é o maior risco de conclusão errada | 3 h |

### 🥉 Faixa 3 — benefício alto, custo alto (ou bloqueado)

| # | Tarefa | Observação |
|---|---|---|
| 9 | **Trava de dado pessoal que varre DADO** | Dívida nº 1 do TODO; o dono adiou para 18/08. Bloqueia a amostragem dos 200 documentos do acervo |
| 10 | **Migration 0071 na Neon** | Bloqueada até 01/09. Enquanto não roda, os 6 ETLs do GitHub reintroduzem convênio duplicado |
| 11 | **Painel de edição (CMS), fase 1** | Plano medido, nada implementado. O ciclo editar→publicar é de 15–20 min por decisão de arquitetura |
| 12 | **Bases de clima e risco** | Nada implementado. O achado é sobre gente: **1.377.577 pessoas expostas** em MG. Geometria do BATER atrás de desafio Cloudflare |
| 13 | **Arquivo de fontes em R2** | Nada implementado. Primeiro passo é medir quantas das 6.378 normas têm `link_pdf` |

### Faixa 4 — pedidos que dependem de decisão do dono

| # | Tarefa | O que falta decidir |
|---|---|---|
| 14 | Apagar `.md` antigos | Quais buckets (4 propostos; o de arquivos *untracked* na raiz do vault não volta pelo git) |
| 15 | Verificar os 24 imóveis da União | O agente foi interrompido antes de produzir arquivo; refazer |
| 16 | Coletor de notícias diário no servidor | Escrito e rodando à mão; agendar depende do servidor |
| 17 | Movimento no topo da hero de `/paraopeba/quem-atua` | Pedido de ontem, nunca executado — ficou soterrado pela confusão das duas cópias do globo |
| 18 | Resumir contratos/PLs/convênios truncados | Escopo não definido: quantos, quais listagens |

---

## Feito hoje (para não se perder)

Nove commits, todos pushados, **nenhum no ar** — ver o gargalo acima.

- **4 camadas de alerta ligadas** (`d1777b3`) — estavam calculadas e invisíveis
  havia dois dias. E o alerta quilombola × mancha **deixou de ser zero**: 6
  sobreposições em 3 territórios, sob barragens da Kinross e da Salitre.
- **Faixa de 8 km × SIGMINE** (`557c0ac`, `4cfd981`) — **1.899 processos que a
  interseção pura não vê**. A Aldeia Katurama, zero em todo alerta de
  sobreposição, tem 20 minas em operação dentro dos 8 km.
- **8.940 normas federais** (`0d8480b`) — a proteção animal aparece (Lei
  5.197/1967, Lei 9.605/1998). Carregadas só em banco local.
- **Registro de protocolo de LAI** (`76940c4`) — nada gravava isso.
- **Barra de destaques** (`8719c13`) e **sanfona do painel** (`4c8e54f`).
- **Unificação de Curvelo** (`94be872`) — as 12 áreas que estavam nos dois
  arquivos e desenhavam duas vezes.
- **Cor corrigida** (`a569341`) — eu tinha medido em HSL num arquivo que
  declara OKLCH, e ia pintar uma camada medida quase da cor do "dado
  inventado".

## Três erros meus, registrados porque se repetem

1. **Trabalhei o dia inteiro na cópia errada do globo.** O publicado é
   `apps/web/public/terras/globo`; editei `terras-devolutas/backend/static/globe`.
   Causa: comecei antes de ler `docs/`.
2. **Um commit meu engoliu arquivos de outra sessão** — `git commit` leva tudo
   que está em *staging*, não só o que acabei de adicionar.
3. **Medi cor no espaço errado** (HSL contra um arquivo em OKLCH), e a
   conclusão saiu invertida com toda a aparência de rigor.

Todos os três têm a mesma raiz: agir antes de ler o que o repositório já diz.

---

# Fila nova — pedidos do dono em 15/08, à tarde

> Entram DEPOIS de tudo que está ranqueado acima. Onde existe endpoint, ele foi
> **chamado** nesta data e o número está aqui; onde não consegui chamar, está
> escrito que não consegui. Nenhum número desta seção foi copiado de release
> de imprensa.

## N1. Termo de uso e origem dos dados (LGPD) — 🥇 faixa 1, e ficou urgente hoje

**Pedido:** um termo explicando de onde vêm as fontes, que é dado público, que
o portal tenta mascarar dado sensível, e um canal para quem vir algo pedir
correção.

**Por que subiu para a faixa 1:** hoje, 15/08, um **CPF real de pessoa física**
foi publicado neste repositório — que é público — dentro da ementa oficial de
uma norma do MMA (`etl/betim/dados/legislacao-mma.json`, ementa de um TAC do
IBAMA no Pará). A trava de dado pessoal (`ef9afe7`) pegou, o commit `7b9c9db`
removeu, e o outro PC mascarou a ingestão na origem. O caso prova a tese do
termo: **o portal republica ato oficial, e ato oficial às vezes traz dado
pessoal dentro dele.** Sem canal de contato declarado, a pessoa citada não tem
para onde escrever.

**O que já existe, e por que não basta.** Há duas páginas de privacidade
(`/judiciario/privacidade` e `/[municipio]/privacidade`), e as duas respondem a
outra pergunta: o que o site **não coleta de quem visita** (sem login, sem
rastreador, sem publicidade). O que falta é o outro lado — o dado das pessoas
**que aparecem no conteúdo publicado**, que nunca visitaram o site e não
escolheram estar nele. São documentos diferentes; o novo não substitui esses.

**Conteúdo mínimo proposto:**

1. **De onde vem cada coisa** — apontando para os `docs/FONTES-*.md` que já
   existem, não repetindo a lista (que envelheceria em duas semanas).
2. **Base legal**: dado público sob LAI (Lei 12.527/2011) e Decreto
   8.777/2016; a LGPD (Lei 13.709/2018) admite tratamento para exercício
   regular de direitos e para transparência. Citar dispositivo, não "conforme a
   legislação".
3. **Licença por fonte, incluindo as restritivas** — o INCRA veda uso
   comercial (`AccessConstraints` do GetCapabilities, registrado em
   `ingerir_incra_quilombolas.py`); o CSV do Palmares é CC-BY. Declarar isso
   protege o projeto e quem reusa.
4. **O que o portal tenta mascarar, e a confissão de que a trava é imperfeita**
   — dizer que ela é automática, que roda por mod-11 e regex, e que **já falhou
   uma vez** (com a data). Um termo que promete infalibilidade fica falso na
   primeira falha; um que declara o limite continua verdadeiro.
5. **Canal de correção**, com prazo de resposta que o projeto consiga cumprir.
   Sem endereço de e-mail funcionando, este item não vai ao ar — melhor não ter
   a página que ter uma que promete resposta e não responde.
6. **Como pedir remoção**, separando o que o portal pode tirar (a cópia local)
   do que não pode (o ato na fonte oficial, que continua público).

**Amarrar com dívida antiga:** o TODO registra que o link "Termos" dava **404
em toda página do eixo Cidades**. Esta página é o destino que faltava.

**Custo:** 3 h para o texto, mais a decisão do dono sobre o canal de contato.
Não depende de banco nem de build — é a tarefa de maior benefício por hora
desta lista inteira.

## N2. Lei Rouanet e os incentivadores — **API medida hoje, e é a melhor da lista**

**Pedido:** para onde vai o orçamento das fundações culturais de grandes
empresas, via Lei Rouanet e incentivos fiscais.

A API do SALIC responde, sem chave, e tem exatamente a trilha pedida.
**Chamadas reais de 15/08/2026:**

| Chamada | Resultado medido |
|---|---|
| `GET /api/v1/projetos?UF=MG&limit=1` | **7.206** projetos em Minas |
| `GET /api/v1/incentivadores?sort=total_doado&limit=1` | **113.548** incentivadores; o maior é **BANCO DO BRASIL SA**, com **R$ 648.387.436,40** doados |
| `GET /api/v1/incentivadores/{id}/doacoes` | link vem pronto em `_links.doacoes` de cada incentivador |

Base: `https://api.salic.cultura.gov.br`. Sem autenticação. `limit` de 1 a 100,
com `offset`. Saída em HAL+JSON, XML ou CSV. Filtros úteis: `UF`, `municipio`,
`ano_projeto`, `area`, `segmento`, `PRONAC`, `cgccpf`.

**É o encaixe mais direto que o portal já teve com uma fonte nova:** o eixo
Cidades já pergunta "quem ganhou contrato desta prefeitura"; `incentivadores →
doações → projeto → município` responde a mesma pergunta com dinheiro que
**deixou de ser imposto**. E `cgccpf` permite cruzar o incentivador cultural
com o fornecedor de contrato público que o portal já guarda — é a mesma
empresa, em dois papéis.

**Quatro armadilhas, todas medidas hoje, não supostas:**

1. **Barra final devolve 301.** `/api/v1/projetos/?UF=MG` redireciona para a
   versão sem barra. Sem `curl -L` (ou `redirect: "follow"`) o cliente recebe
   HTML de Apache e não JSON.
2. **`_links.self` e `_links.doacoes` do mesmo incentivador trazem hashes
   DIFERENTES.** Reaproveitar o id do `self` para montar a URL de doações à mão
   dá 404. Seguir o link publicado, nunca concatenar.
3. **Mojibake na origem.** "Brasília" chegou como `Bras?lia` — o corpo não está
   em UTF-8 limpo. Ler forçando a codificação e **abortar se sobrar caractere
   de substituição**, que é a regra que `extrair-educacao-paraopeba.mts` já
   aplica.
4. **113.548 incentivadores a 100 por página são 1.136 requisições.** Isso é
   coleta agendada com retomada, não script de uma passada — e o teto de egress
   do Neon manda começar por MG (7.206 projetos), não pelo Brasil.

**Custo:** 1 h para uma sonda que mede o volume real de MG; 6–8 h para o ETL
com retomada; a tela depois. Não depende do deploy.

## N3. Incentivo fiscal ao esporte — **não medido, e é o mais fraco da lista**

`https://www.gov.br/esporte/pt-br/acesso-a-informacao/lei-de-incentivo-ao-esporte`
devolveu **HTTP 401** para agente que não é navegador. Não sei ainda se existe
API, dump ou só página.

**Primeiro passo, antes de qualquer plano:** achar se a Lei de Incentivo ao
Esporte (Lei 11.438/2006) publica em `dados.gov.br` — a Rouanet publica, e o
CKAN federal é o lugar provável. **Não escrever plano de ingestão antes disso.**
Este item nasce declaradamente sem número, ao contrário do N2.

## N4. Comunica BR — federal por município

`https://comunicabr.presidencia.gov.br` responde **200** (sem a barra final; a
tentativa com barra falhou uma vez, então o coletor precisa tolerar as duas).
Portal da Secom, versão nova de janeiro/2026, cobre os **5.570 municípios** e
oferece PDF por município e por mês.

**O que não consegui determinar:** se existe API JSON por trás. O HTML servido
por `curl` não expõe rota `/api/`, e a leitura assistida caiu com `ECONNRESET`.
Pode ser render no servidor (aí a coleta é HTML, mais frágil) ou uma API que só
aparece com o JS rodando.

**Primeiro passo:** abrir o portal no navegador do preview, filtrar por Betim,
e **ler a aba de rede** — é assim que se descobre a API real, e é a mesma
técnica que achou o `/env.json` das SPAs da PBH (registrado no vault). Uma hora
de trabalho decide se este item é barato ou caro; hoje não dá para dizer qual.

**Ressalva de conteúdo, para quando entrar:** o ComunicaBR é comunicação de
governo sobre a própria atuação. Serve como **índice de para onde apontar** — e
o portal deve cruzar com a fonte orçamentária (Portal da Transparência, SIAFI),
não republicar o valor da Secom como se fosse execução auditada. Publicar
número de comunicação oficial com a mesma cara de dado orçamentário é o tipo de
erro que o projeto inteiro existe para não cometer.

## N5. normas.leg.br / LexML

**Medido hoje:** `https://normas.leg.br/` responde 200. `https://normas.leg.br/api`
devolve **404 com `content-type: application/json`** — ou seja, há superfície de
API JSON ali; falta descobrir as rotas. Resolução por URN funciona:
`https://normas.leg.br/?urn=urn:lex:br:federal:lei:1998-02-12;9605` devolve 200
(é a Lei de Crimes Ambientais, que o portal já cita).

**A armadilha grande:** o SRU do LexML
(`https://www.lexml.gov.br/busca/SRU?operation=searchRetrieve...`) está atrás de
uma **"Verificação de segurança — Senado Federal"**. É captcha/bot-check, a
mesma parede do TCE-MG. **Planejar em cima do SRU é planejar em cima de algo
que não responde a robô.**

**Caminho recomendado, nesta ordem:**

1. O dataset **"Acervo do portal LexML"** dos dados abertos do Senado
   (`www12.senado.leg.br`, respondeu 200) — arquivo, não requisição por item.
2. **Resolução por URN** para o link de cada norma que o portal já publica —
   isso é barato e vale sozinho: dá endereço canônico e permanente para as
   6.378 estaduais e as 8.940 federais que já estão no acervo.
3. Só então investigar as rotas de `normas.leg.br/api`.

**O ganho real não é mais norma — é URN.** O portal já tem norma demais; o que
ele não tem é identificador estável que sobreviva a mudança de site do órgão.

## N6. Atualizar as propostas de apresentação para redes sociais

A proposta anterior existe e está em
`Obsidian Vault/Projetos/Clareira — Roteiros de Vídeo (Anúncio dos Projetos).md`
— 13 roteiros verticais de até 60 s, de **29/07/2026**, cobrindo os projetos da
Clareira. **Não está neste repositório**, e é por isso que envelheceu sem que
ninguém visse.

**Está desatualizada em três pontos, medidos contra o portal de hoje:**

1. **O endereço mudou.** O roteiro #1 diz `controlepopular.vercel.app/betim`; o
   portal está em `controlepopular.com.br`.
2. **O Controle Popular aparece como três projetos** (#1 Betim, #2 Congresso,
   #3 Judiciário). Hoje são **seis frentes** no ar — entram Função Social da
   Terra, Paraopeba e Direitos em Movimento, que não têm roteiro nenhum. As
   duas frentes mais fortes visualmente (o globo 3D e os alertas de
   sobreposição) são justamente as que ficaram de fora.
3. **Os números envelheceram**: o roteiro do Congresso vende "670 PLs"; o
   acervo cresceu desde então.

**O que aproveitar sem reescrever:** o método daquele arquivo é bom e deve ser
mantido — gancho de 5 s com alternativa e justificativa, tabela com marcação de
tempo, linha "🎥 A prova" dizendo qual captura sustenta a fala, e os três
avisos de honestidade. A regra "**nunca mostre mockup**" vale ainda mais aqui:
as frentes novas têm tela de verdade para mostrar.

**Ganchos que os dados de hoje já sustentam** (cada um com número medido nesta
sessão ou nas anteriores, e todos verificáveis na tela):

- **6 sobreposições** entre território quilombola e mancha de barragem, sob
  barragens da Kinross — o alerta que era zero até alguém ligar a camada.
- **1.899 processos minerários** que a interseção pura não vê e a faixa de 8 km
  vê; a Aldeia Katurama tem **20 minas em operação** dentro dela.
- **Belo Horizonte pontua 0,00 "risco muito baixo"** com 389.218 pessoas em
  área de risco — o caso que obrigou a publicar as componentes em vez do
  índice.
- **A linha do tempo da reparação não continha o rompimento** até 15/08 —
  serve de gancho honesto sobre o próprio método ("o que a gente achou que
  faltava no nosso portal").

**Custo:** 4 h para três roteiros novos (Terra, Paraopeba, Direitos em
Movimento) e a atualização dos três antigos. **Depende de o site estar no ar**,
porque a regra é gravar tela real.

---

## Resposta a uma pergunta do dono: o painel de edição já roda fora do Claude Code?

**Roda — mas é linha de comando, não painel gráfico.** A Fase 1 está entregue
no commit `acdfef6`:

```bash
npx tsx scripts/editar-pagina.mts --listar
```

Para editar título e descrição de uma página, com autoria e motivo
obrigatórios:

```bash
npx tsx scripts/editar-pagina.mts --rota /bh/saude --titulo "Saúde em BH" --por "Artur" --motivo "faltava o nome da cidade"
```

Três coisas que valem saber antes de usar:

- **Ele não publica.** Grava em `apps/web/data/edicoes.json` na hora; o site só
  muda no próximo build. O plano mede esse ciclo em **15 a 20 minutos** no
  tamanho atual (3.850 páginas), e não existe build parcial.
- **A trilha é o git.** `git log -p apps/web/data/edicoes.json` responde quem
  mudou o quê e por quê; `git revert` desfaz. Por isso `--por` e `--motivo` são
  obrigatórios.
- **O painel web foi adiado de propósito, não esquecido.** `docs/PLANO-PAINEL-
  EDICAO.md` dedica uma seção a por que ele não pode estar na internet; um CLI
  na máquina de build resolve o mesmo caso com superfície de ataque zero. O
  painel gráfico continua na fila (item 11 do ranking) para quando alguém que
  não usa terminal precisar editar — **e é aí que ele deve ser retomado, porque
  hoje só o dono consegue editar.**
