{
  "name": "@cp/web",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "dev": "next dev",
    "//indice:busca": "Regenera o indice estatico da /busca a partir do banco. Entra no prebuild porque indice que nao acompanha o build vira busca que responde sobre o acervo de ontem — e sem erro nenhum. `--env-file-if-exists` e nao `--env-file`: no CI nao existe .env.local, as variaveis vem do ambiente, e a flag estrita abortaria o build.",
    "indice:busca": "tsx --env-file-if-exists=.env.local scripts/gerar-indice-busca.mts",
    "prebuild": "node scripts/limpar-fetch-cache.mjs && npm run indice:busca",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "test": "vitest run",
    "cf:build": "opennextjs-cloudflare build",
    "//cf:preview": "populateCache NAO eh opcional: sem ele o dir cdn-cgi/_next_cache nao existe nos assets, e toda rota SSG sob segmento dinamico com dynamicParams=false devolve 404/NoFallbackError. `build` sozinho nao popula.",
    "cf:preview": "opennextjs-cloudflare build && opennextjs-cloudflare populateCache local && opennextjs-cloudflare preview",
    "cf:deploy": "opennextjs-cloudflare build && opennextjs-cloudflare populateCache remote && opennextjs-cloudflare deploy"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.0",
    "docx": "^9.5.1",
    "drizzle-orm": "^0.44.0",
    "lucide-react": "^1.25.0",
    "next": "16.2.12",
    "next-themes": "^0.4.6",
    "pdf-lib": "^1.17.1",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@opennextjs/cloudflare": "^1.20.2",
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/pg": "^8.21.0",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "drizzle-kit": "^0.31.0",
    "eslint": "^9",
    "eslint-config-next": "16.2.12",
    "pg": "^8.23.0",
    "tailwindcss": "^4",
    "tsx": "^4.23.1",
    "typescript": "^5",
    "vitest": "^4.1.10",
    "wrangler": "^4.115.0"
  }
}
