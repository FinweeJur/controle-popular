import { criarLinkDaZona } from "@/lib/link-zona";
import { BASE_PATH } from "@/lib/judiciario/basePath";

/** `<Link>` da zona /judiciario — ver `lib/link-zona.tsx`. */
export default criarLinkDaZona(BASE_PATH);
