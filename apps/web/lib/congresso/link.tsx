import { criarLinkDaZona } from "@/lib/link-zona";
import { BASE_PATH } from "@/lib/congresso/basePath";

/** `<Link>` da zona /congresso — ver `lib/link-zona.tsx`. */
export default criarLinkDaZona(BASE_PATH);
